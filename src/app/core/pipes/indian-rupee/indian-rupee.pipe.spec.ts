import { IndianRupeePipe } from './indian-rupee.pipe';

describe('IndianRupeePipe', () => {
  it('create an instance', () => {
    const pipe = new IndianRupeePipe();
    expect(pipe).toBeTruthy();
  });
});
